<div class="container">
  
<!DOCTYPE html>
<html>
<div class="flash">
  <%= flash[:notice] %>
</div>
<div class="row">
  <div class="col-md-3">
<!--ここから-->
<%= render 'sideuser', user: @user %>


<%= render 'sidenew', newbook: @newbook %>



<!--ここまで-->
</div>
  <div class="col-md-8 offset-md-1">

<h4>Book detail</h4>

<table>
  <thead>
    <tr>
      <th>  </th>
      <th>Title</th>
      <th>Opinion</th>
    </tr>
  </thead>
  <tbody>
      <tr>
       <td><%= link_to user_path(@book.user.id) do %>
          <%= image_tag @book.user.get_profile_image(100,100) %></br>
          <%= @book.user.name%>
            <% end %>
        </td>
        <td><%= link_to @book_path do %>
          <%= @book.title %>
            <% end %>
        </td>
        <td><%= @book.body %></td>

        <% if @book.user == current_user %>
            <td><%= link_to "Edit", "/books/#{@book.id}/edit"%></td>
            <td><%= link_to "Destroy",  book_path(@book), method: :delete, "data-confirm" => "Are you sure?"%>
            <% end %>
        </td>
      </tr>
  </tbody>
</table>
</html>
</div>
</div>
</div>